version: 2

models:
  - name: int_user_details_address
    description: "data about users and their geos etc"
    columns:
      - name: user_id
        description: "The primary key for this table"
        tests:
          - unique
          - not not_null
